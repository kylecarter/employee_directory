EmployeeDirectory.search = do ->
  'use strict'

  config = new Object()
  state = new Object()
  selectors = new Object()

  configModule = ()->
    return

  initModule = ()->
    return

  {
    configModule: configModule
    initModule: initModule
  }
